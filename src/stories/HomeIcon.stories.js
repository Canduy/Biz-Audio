import { IconHome } from "../components/Icon";

export default {
  title: "Icon/IconHome",
  component: IconHome,
};

const Template = (args) => <IconHome />;

export const HomeIcon = Template.bind({});
HomeIcon.args = {};
