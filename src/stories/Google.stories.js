import { GoogleIcon } from "../components/Icon";

export default {
  title: "Icon/GoogleIcon",
  component: GoogleIcon,
};

const Template = (args) => <GoogleIcon {...args} />;

export const IconGoogle = Template.bind({});
