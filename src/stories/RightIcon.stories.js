import { RightIcon } from "../components/Icon";

export default {
  title: "Icon/RightIcon",
  component: RightIcon,
};

const Template = (args) => <RightIcon {...args} />;

export const IconRight = Template.bind({});

IconRight.args = {};
