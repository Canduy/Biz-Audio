import Topic from "../layouts/Topic/Topic";

export default {
  title: "Layout/Topic",
  component: Topic,
};

const Template = (args) => <Topic {...args} />;
// export const TopicLayout = Template.bind({});

// TopicLayout.args = {};
